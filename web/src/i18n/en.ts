/**
 * What the dashboard says, in English.
 *
 * Typed against the Chinese catalogue, so a message added there and forgotten
 * here is a compile error rather than a Chinese sentence in an English page.
 * The metric vocabulary is the CLI's and stays as it is: `I/M`, `T`, `Q`, and a
 * bare `¥12.34` for money.
 */

import type { Messages } from './zh';

export const en: Messages = {
  app: {
    loading: 'Loading…',
    loadingHint: 'Asking the local service for data.',
    requestFailed: 'Request failed',
    noProject: 'No such project',
    noProjectHint: 'This project is not in the current filter — an agent or the time range may have excluded it.',
    sidebar: 'Toggle the project tree',
  },
  filters: {
    tagline: 'local usage analysis',
    agentButton: (value: string) => `agent: ${value} ▾`,
    all: 'all',
    allAgents: 'all agents',
    search: 'Search projects / paths',
    snapshot: 'offline snapshot',
    scanned: (ago: string) => `scanned ${ago}`,
    summary: (agents: string, money: string, range: string) => `${agents} agents · ${money} · ${range}`,
    rescan: 'Rescan',
    rescanning: 'Rescanning…',
    rescanHint: 'POST /api/refresh: read every agent’s data directory again',
    theme: 'Switch the light/dark theme',
    language: 'Language',
    languageHint: 'Switch between Chinese and English — written to the configuration file, so the CLI speaks it too',
    switching: 'Switching…',
  },
  range: {
    today: 'today',
    week: 'this week',
    month: 'this month',
    all: 'all time',
  },
  ago: {
    seconds: (value: string) => `${value}s ago`,
    minutes: (value: string) => `${value} min ago`,
    hours: (value: string) => `${value} h ago`,
    days: (value: string) => `${value} d ago`,
  },
  tree: {
    title: 'Projects',
    all: 'All',
    none: 'No project matches.',
    total: (money: string, requests: string) => `${money} in total · Q ${requests}`,
    repoHint: 'a git repository (all its worktrees)',
    pathHint: 'a single directory',
    counts: (sessions: string, subagents: string, workspaces: string) =>
      `${sessions} sessions / ${subagents} subagents / ${workspaces} workspaces`,
    workspaceCounts: (sessions: string, subagents: string) => `${sessions} sessions / ${subagents} subagents`,
    last: (instant: string) => `last ${instant}`,
    own: 'own',
    spawned: 'spawned',
    expand: 'Expand',
    collapse: 'Collapse',
    view: 'Open',
    untitled: (id: string) => `(untitled session ${id})`,
    archived: 'archived',
    subagents: (count: string) => `${count} subagents`,
    noUsage: 'This workspace recorded nothing in the current range.',
  },
  scope: {
    tabs: {
      overview: 'Overview',
      projects: 'Projects',
      agents: 'agents',
      sessions: 'Sessions',
      usage: 'Usage',
      models: 'Models & pricing',
    },
    allProjects: 'All projects',
    projectsCount: (count: string) => `${count} projects`,
    workspaces: (count: string) => `${count} workspaces`,
    repo: 'git repository',
    path: 'directory',
    back: 'Back to all projects',
    loading: 'Loading…',
    projectAgents: (name: string) => `agents · ${name}`,
    projectSessions: (name: string) => `Sessions (${name})`,
    projectWorkspaces: (name: string) => `Workspaces · ${name}`,
  },
  vocabulary: {
    requests: 'requests billed',
    inputMiss: 'cache-miss input',
    cacheRead: 'cache-hit input',
    cacheWrite: 'cache-write input',
    inputTotal: 'input total = I/M + I/C + I/W',
    outputOnly: 'output without reasoning',
    reasoning: 'reasoning (inside O/T)',
    outputTotal: 'output total = O + R',
    tokens: 'billed tokens = I/T + O/T',
    cost: 'total cost',
    recent: 'the last billed request',
    hitRate: 'cache-hit input ÷ input total (I/C ÷ I/T)',
    costHint: 'total cost (cache-hit money is the I/C line’s money, never a second line)',
    reasoningHint: 'reasoning (a part of the output, already taken out of O)',
    outputOnlyHint: 'the output that is not reasoning',
    shareOfInput: (share: string) => ` (${share} of I/T)`,
    shareOfRow: (share: string) => `${share} of this row`,
    shareOfMetric: (share: string) => `${share} of this metric`,
    shareOfTotal: 'this bucket as a share of the row’s T',
    ownMoney: 'every cell is its own money',
    ownMoneyShort: 'each item’s own money',
    total: 'total',
  },
  kpi: {
    money: 'total cost in this range',
    requests: 'requests billed',
    requestsHint: (sessions: string, subagents: string) => `${sessions} sessions (${subagents} subagents)`,
    unpriced: (count: string) => `${count} unpriced`,
    tokens: 'billed tokens = I/T + O/T',
    hitRate: 'I/C share',
    hitRateHint: 'cache-hit input ÷ input total (I/C ÷ I/T)',
    cacheRead: (value: string) => `I/C ${value}`,
    inputTotal: (value: string) => `I/T ${value}`,
    reasoning: (value: string) => `R ${value}`,
    cacheWrite: (value: string) => `I/W ${value}`,
  },
  overview: {
    composition: 'Composition',
    buckets: 'Bucket',
    tokens: 'tokens',
    shareOfTotal: 'of T',
    compositionNote: (tokens: string, money: string) =>
      `Bar length is the share: the top bar by T, the bottom one by money. The five parts never overlap and add up to exactly T (I/M + I/C + I/W + O + R = T, with O = O/T − R). Total ${tokens} tokens · ${money}`,
    series: 'Over time',
    byDay: 'By day',
    byHour: 'By hour',
    emptyHour: 'Nothing in the last 14 days (hourly detail is kept for 14 days).',
    empty: 'Nothing in this range.',
    seriesNote: (first: string, last: string) =>
      `Data from ${first} to ${last} · priced per (time bucket × agent × project) and then added up`,
    seriesHourNote: ' · hourly detail covers the last 14 days',
    topProjects: 'Projects (top five)',
    topSessions: 'Sessions (top five)',
    topSessionsNamed: (name: string) => `Sessions (top five) · ${name}`,
    allRanked: (count: string) => `all ${count} (sortable) →`,
    warnings: (count: string) => `${count} warnings`,
  },
  usage: {
    scope: 'Usage in this scope',
    scopeRule: 'own + spawned = total',
    columns: { range: 'Scope', hitRate: 'I/C share' },
    scopeNote: (share: string) =>
      `Spawned share ${share}; same figures as the CLI’s usage --subagent, same abbreviations (Q I/M I/C I/W I/T O R O/T T).`,
    detail: 'Every figure',
    detailOpen: 'Collapse',
    detailClosed: 'Show the ten figures',
    detailHint: 'The CLI’s ten figures (I/M I/W I/C I/T O R O/T T Q and the total) live here — expand to read or copy them column by column.',
    sources: 'Data sources',
    pricing: (label: string, provider: string, currency: string) => `Pricing ${label} (${provider}) · ${currency}`,
    scan: (ms: string) => `scan ${ms} ms`,
    root: 'Data root',
  },
  board: {
    sortBy: 'Sort',
    descending: 'most first',
    ascending: 'least first',
    descendingShort: 'desc ↓',
    ascendingShort: 'asc ↑',
    descendingWord: 'desc',
    ascendingWord: 'asc',
    barRule:
      'A bar is this row’s own token make-up: five disjoint billing items (I/M, I/C, I/W, O, R), the whole bar being this row’s 100%. A zero item is not drawn; a sliver under 1.2% is widened to 1.2% so it stays visible. Sorting lives in the dropdown at the top right.',
    barTitle: 'this row’s token make-up (the whole bar is this row’s 100%)',
    rowNote: (metric: string, direction: string) =>
      `The bar is this row’s own token make-up (the whole bar is 100%, it says nothing about size — size is the number on the right); under it, every bucket with its tokens and money (same abbreviations as the CLI). Sorting is the 「${metric}${direction}」 dropdown; click a row to open all of its buckets.`,
    total: (money: string) => `${money} in total`,
    emptyMetric: 'This metric is zero everywhere.',
    empty: 'Nothing in this range.',
    charts: 'Charts',
    chartsOpen: 'Hide charts',
    chartsHint: 'one bar chart per metric: who owns this total',
    chartsNote: (rows: number) =>
      `One horizontal bar chart per metric: add that metric up over every entry of the list and see who contributed the total. The bar is that entry’s share of the metric’s total (a full bar is all of it), with the value and the share on the right. The card names the top ${rows}; 「show all」 lists every entry under that metric. A row’s own make-up is the bar inside the row.`,
    showMore: (count: string) => `Show all ${count}`,
    folded: (count: string, sum: string) => `${count} more: ${sum}`,
    allListed: (count: string) => `all ${count} listed`,
    metricTotal: 'the metric’s total',
    dialogLabel: (metric: string) => `${metric}: every entry`,
    dialogTitle: (metric: string, count: string) => `${metric} · all ${count}`,
    dialogNote: (hint: string, total: string) => `${hint}｜total ${total} (bar = share of the metric’s total)`,
    close: 'Close',
    sessionCounts: (sessions: string, subagents: string) => `${sessions} sessions · ${subagents} subagents`,
    sessionCountsProject: (sessions: string, subagents: string) => `${sessions} sessions (${subagents} sub)`,
    repoSubtitle: (workspaces: string) => `git repository · ${workspaces} workspaces`,
    subagentsChip: (count: string) => `${count} sub`,
    agentFootnote: 'Click a row to open every bucket; the money is the agent’s own.',
    projectFootnote: 'Click a row to open every bucket of that project; click the name to open its page.',
    workspaceFootnote: 'Click a row to open every bucket of that workspace; the money is its own.',
    noAgents: 'No agent was read in this range.',
    noProjects: 'No project in this range.',
  },
  session: {
    title: 'Sessions',
    untitled: (id: string) => `(untitled) ${id}`,
    untitledNamed: (id: string) => `(untitled session ${id})`,
    first: 'first',
    last: 'last',
    own: 'own',
    spawned: 'spawned',
    open: 'Open the session →',
    table: 'Table view',
    tableHint: 'Switch between the ranking and the table',
    flat: 'With subagents',
    fold: 'Fold subagents',
    flatHint: 'List subagents as rows too (by default they are folded into their parent)',
    flatNote: 'Subagents included: a subagent row and its parent count the same usage twice.',
    foldedNote: 'One row per delegation subtree (the session plus everything it spawned).',
    rowNote: 'Click a row to open every bucket and the money of that session.',
    share: (money: string, hit: string) => `${money} in total, I/C at ${hit}.`,
    hitRate: 'I/C ÷ I/T',
    notFound: 'This session cannot be read',
    pick: 'Pick a session',
    pickHint: 'Click a session (or a subagent) in the tree on the left to see its details.',
    main: 'main',
    subagentNode: 'subagent',
    archived: 'archived',
    project: (name: string) => `Project ${name}`,
    created: (instant: string) => `created ${instant}`,
    span: (first: string, last: string) => `usage from ${first} → ${last}`,
    parent: 'Parent: ',
    back: 'Back',
    moneyTitle: 'total cost of this session (own + spawned)',
    requestTitle: 'requests billed',
    requestsHint: (own: string, spawned: string) => `own ${own} · spawned ${spawned}`,
    tokensTitle: 'billed tokens = I/T + O/T',
    spawnedLabel: 'Spawned',
    noSpawned: 'no subagents were spawned',
    spawnedShare: (share: string) => `${share} of this session’s cost`,
    delegation: (count: string) => `Delegation tree (${count} direct subagents)`,
    models: 'Models in this session',
    bands: 'Price bands in this session',
  },
  tables: {
    agents: 'Per agent',
    sessionsColumn: 'sessions (sub)',
    agentsHint: (lean: boolean) =>
      lean ? 'keep requests / total / money / share only' : 'show every bucket',
    lean: 'Lean columns',
    full: 'All columns',
    modes: { tokens: 'counts', cost: 'money', share: 'shares' },
    totalRow: 'Total',
    agentNote: (detailed: boolean) =>
      detailed
        ? 'The four buckets never overlap: I/T = I/M + I/W + I/C, R is inside O/T, and O/T = O + R. Switched to money, every cell is that item’s own.'
        : 'By default: Q, T, money and share only; 「expand the buckets」 adds I/M I/W I/C I/T O R O/T, switchable between counts, money and shares.',
    unpriced: (count: string) => `${count} records have no matching model in the price list and are not counted in the cost.`,
    sessionsTitle: 'Session detail',
    sortHint: 'click a heading to sort',
    leanHint: (lean: boolean) => (lean ? 'bring back every metric column' : 'keep the main columns only (a narrower table)'),
    noSessions: 'No session in this range.',
    sessionsNote: (rows: string, shown: number | null, total: string) =>
      `${rows} rows${shown === null ? '' : `, showing the first ${shown}`}; ${total} in total.`,
    bucketsTitle: 'The five token buckets',
    bucketsNote: 'Shares are taken over the four billing buckets (I/T + O/T = T); R is already inside O/T and is never billed again.',
    modelsTitle: 'Models',
    modelsModel: 'Model',
    noModels: 'No priced model in this range.',
    bandsTitle: 'Price bands',
    bandsModel: 'Model · price period',
    bandsWindow: 'Window',
    bandsTier: 'Tier',
    tiers: { peak: 'peak', offPeak: 'off-peak', flat: 'flat' },
    fallback: 'fallback',
    fallbackHint: (resolution: string) => `this band was matched by falling back: ${resolution}`,
    noBands: 'No price band matched in this range.',
    item: 'Charged item',
    unitPrice: 'Rate',
    excess: (tokens: string, rate: string) => `above ${tokens} tokens: ${rate}`,
    ttl: (tier: string, multiplier: string) => `${tier} cache write ×${multiplier}`,
    seriesCost: 'money',
    seriesRequests: 'Q',
    seriesTokens: 'T',
    chartTotal: 'Total',
  },
};
