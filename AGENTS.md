# git  
没有要求commit时不commit，如果完成一个任务需要多步，可以适当commit多次。
commit message不要太复杂，co-author 当前模型。

# 协作
我提问时先分析、讨论，不要直接改代码，确认后再动手。
需要多轮的分析稿、决策与待办写进 `.agents/drafts/`（已被 gitignore 忽略），不要只留在对话里。
结论里的数字都要带测量时间——日志、语料都是活的，隔一会儿就变。
